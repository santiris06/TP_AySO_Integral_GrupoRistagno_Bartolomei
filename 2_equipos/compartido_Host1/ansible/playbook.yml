---
- hosts: all

  # Variables Globales
  vars_files:
    - vars/main.yml

  tasks:
    - include_role:
        name: TP_INI
    
    - name: "prueba ping"
      ansible.builtin.ping:
    - name: "otra prueba"
      debug:
        msg: "Despues de la ejecucion del rol"

